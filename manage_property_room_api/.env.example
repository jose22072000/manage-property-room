# Server
PORT=8080
ENV=dev

# Database
DB_DRIVER=sqlite
DB_DSN=./data/app.db

# JWT
JWT_SECRET=change-me-in-production-please-change-me
JWT_TTL=24h

# CORS (comma-separated; * for any in dev)
CORS_ORIGINS=*
